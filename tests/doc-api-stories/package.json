{
  "name": "@superdoc-testing/doc-api-stories",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "pnpm --dir ../.. corpus:pull",
    "test": "pnpm --prefix ../../apps/cli run build && pnpm --prefix ../../packages/sdk/langs/node run build && vitest run --config ./vitest.config.ts"
  },
  "dependencies": {
    "@superdoc-dev/sdk": "file:../../packages/sdk/langs/node"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
